import { Player } from '@/types';

// Calculate team overall rating
export function calculateTeamOverallRating(players: Player[]): number {
  // TODO: Implement team rating calculation (already exists in ratings.ts)
  return 0;
}
